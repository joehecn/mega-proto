
# EditorConfig is awesome: http://EditorConfig.org

# root 表明是最顶层的配置文件，发现设为true时，才会停止查找.editorconfig文件
# 当打开一个文件时，EditorConfig插件会在打开文件的目录和其每一级父目录查找.editorconfig文件，直到找到一个root = true

# [*] 对所有文件生效(ps: [*.md] - 对后缀名为 md 的文件生效)
# charset 文件编码
# indent_style 缩进类型
# indent_size 缩进数量
# end_of_line 换行符格式
# insert_final_newline 是否在文件的最后插入一个空行
# trim_trailing_whitespace 是否删除行尾的空格

root = true

[*]
charset = utf-8
indent_style = space
indent_size = 2
end_of_line = lf
insert_final_newline = true
trim_trailing_whitespace = true
